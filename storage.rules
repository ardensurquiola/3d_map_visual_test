rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read for CSV files
    match /csvs/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
